class file {
  constructor(url, title, folderId) {
    this._url = url; // indirizzo per prelevare il file
    this._fileId = id; //identificativo file
    this._title = title; // [obbligatorio] titolo
    this._folderId = folderId; //[opzionale] id della cartella dove verrà caricato il file
  }
}
