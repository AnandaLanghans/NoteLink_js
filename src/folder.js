class folder {
  constructor(istitution, course, argument) {
    this.folderId = id; // identificativo della cartella
    this.istitution = istitution; //categoria della cartella (università)
    this.course = course; // categoria della cartella (corso)
    this.argument = argument; //categoria della cartella (argomento)
  }
}
